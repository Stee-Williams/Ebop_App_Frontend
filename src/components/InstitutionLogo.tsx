const InstitutionLogo = ({ className = "" }: { className?: string }) => {
  return (
    <img src="/images/dgcpt.png" alt="logo du trésor"  className="h-32 w-auto mx-auto object-contain"/>
  );
};

export default InstitutionLogo;
